<div class="attendees">
  <h3 class="heading">attendees:</h3>
  <ul>
    <% @attendees.each do |attendee| %>
      <li class='attendee'>
        <div class='attendee-name'>
          <%= link_to "- #{attendee.name}", root_url(:subdomain => attendee.filename) %>
        </div>
      </li>
    <% end %>
  </ul>
</div>
