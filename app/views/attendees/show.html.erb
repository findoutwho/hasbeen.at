<div class="attendee-detail">
  <div class="avatar">
    <img src="<%= @attendee.avatar %>" />
  </div>
  <div class="name">
    <span class="property">name: </span><span class="value"><%= @attendee.name %></span>
  </div>
  <div class="profile">
    <div class="property">profile:</div>
    <ul>
      <li>- <a href="<%= @attendee.web %>"><span class="value"><%= @attendee.web %></span></a></li>
      <li>- <a href="https://twitter.com/<%= @attendee.twitter %>"><span class="value">@<%= @attendee.twitter %></span></a></li>
    </ul>
  </div>
  <div class="event-list">
    <ul>
      <% @attendee.events.each do |year_of_events| %>
        <% year_of_events.fetch("events").each do |event| %>
          <li>
            <% if event.respond_to?(:fetch) %>
              <div class="value">
                <span class="role"><%= event.fetch('role') %></span>
                <%= event.fetch('name') %> (<%= year_of_events.fetch("year") %>)
              </div>
            <% else %>
              <div class="value">
                <span class="role">Attendee</span>
                <%= event %> (<%= year_of_events.fetch("year") %>)</span>
            </div>
          <% end %>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
  <div class="join-link">
    Join hasbeen.at <a href="https://github.com/schultyy/hasbeen.at/">on GitHub</a>
  </div>
</div>
